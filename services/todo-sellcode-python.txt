import math
import numpy

# --- BEGIN OF "SELL" CODE ---
a=5
b=3.14
c=math.pi
A=numpy.matrix([[1,2],[3,4]])
d=numpy.linalg.det(A)
# --- END OF "SELL" CODE ---

_vars=dir()
_types=[]
_values=[]
for v in _vars:
	if(v.startswith('__') and v.startswith('__'):
		continue
	value = eval(v)
	_types.append(type(value))
	if isinstance(A, numpy.matrix):
		value = numpy.array2string(A, separator=',').replace('\n','')
	_values.append(value)
